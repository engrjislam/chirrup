### Installing flask
-Type "pip install flask" on the command line

### How to setup the client
-cd to the right directory from the command line
-Type "export FLASK_APP=client.py" and then type "flask run"
-Open your browser and type localhost:5000/rooms

